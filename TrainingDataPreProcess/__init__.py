'''
I am the package of processing training data.
General goal:
    given a list of training data, do:
        1, segmentation (call LTP)
        2, bow feature selection
        3, word -> id mapping
        4, generate SVM type training data

'''